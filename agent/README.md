# Agent Module

### Linux Instructions

**1. Build and run:**
```bash
sudo docker-compose up --build
```

**2. Rebuild after changes:**
```bash
sudo docker-compose down
sudo docker-compose up --build
```

The MQTT Broker is exposed on port 1883.
